# `@shared/utils`

> TODO: description

## Usage

```
import {requireStyles, setupApp} from "@shared/utils";
import router from "./router";

requireStyles();

const app = createApp(App);
setupApp(app, router);
app.mount("#app");

setupApp(app, router);
```
