# `@shared/utils`

> TODO: description

## Usage

```
import {setupApp} from "@shared/utils";
import router from "./router";

const app = createApp(App);
setupApp(app, router);
app.mount("#app");

setupApp(app, router);
```
